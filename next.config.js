/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["placeimg.com", "avatars.githubusercontent.com"],
  },
};

module.exports = nextConfig;
